{
    "name": "Job Complete",
    "properties": {
        "type": {
            "type": "string",
            "description": "Worker message type.",
            "required": "true"
        },
        "job": {
            "type": "string",
            "description": "Job ID/GUID.",
            "required": "false"
        },
        "result": {
            "type": "string",
            "description": "Complete result, eg. success|reschedule|failure|error.",
            "required": "false"
        },
        "message": {
            "type": "string",
            "description": "Complete message.",
            "required": "true"
        }
    }
}
