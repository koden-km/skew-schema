{
    "name": "Job Progress",
    "properties": {
        "type": {
            "type": "string",
            "description": "Worker message type.",
            "required": "true"
        },
        "job": {
            "type": "string",
            "description": "Job ID/GUID.",
            "required": "false"
        },
        "progress": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "description": "Job progress percentage (floating point value between 0 and 1).",
            "required": "true"
        }
    }
}
